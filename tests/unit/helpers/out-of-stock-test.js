import { outOfStock } from 'store-n-stuff/helpers/out-of-stock';
import { module, test } from 'qunit';

module('Unit | Helper | out of stock');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = outOfStock([42]);
  assert.ok(result);
});
